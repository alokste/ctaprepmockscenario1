name: DEVELOP_PullRequestValidation_Pipeline
on: 
  pull_request:
    types: [opened, reopened]
    branches:
     - 'develop'
jobs:
  ValidatePullRequest:
    runs-on: ubuntu-latest
    steps:
      - run: echo "pipeline was triggered by event :" ${{ github.event_name }} 
      - run: echo "pipeline is based on remote branch :" ${{github.ref_name}} 
      - uses: actions/checkout@v2
      - uses: sfdx-actions/setup-sfdx@v1
        with:
          sfdx-auth-url: ${{ secrets.AUTH_SECRET_SFORG_cienviron }}
      - name: "installing nodeapp and creating delta package for validation..."
        run: |
          sudo npm install sfdx-git-delta@latest -g
          git config remote.origin.fetch '+refs/heads/*:refs/remotes/origin/*'
          git fetch --all
          git checkout -b pr
          git --no-pager diff --name-status pr origin/develop
          sgd --to pr --from origin/develop --repo . --output .
          cat package/package.xml     
      - name: "running validation of pull request against CI environment..."
        run: sfdx force:source:deploy -x package/package.xml --checkonly --testlevel=RunLocalTests -w30